function winget-upgrade {
    foreach ($arg in $args) {
        if (-not [string]::IsNullOrWhitespace($arg)) {
            & winget upgrade "${arg}"
        }
    }
}

{{ template "powershell/profile.ps1" . }}
